const scenarios={nominal:{label:'NOMINAL OPERATION',status:'Reference condition',title:'Establish the application baseline.',copy:'Send representative sensor reports and telemetry through an available path. Record delivery timing and session behavior before introducing disruptions.',measure:'Message age · Delivery ratio · End-to-end latency',path:'M85 95 L285 55 L495 95 L625 180'},handover:{label:'GATEWAY HANDOVER',status:'Transition condition',title:'Follow the session through a transition.',copy:'Change the gateway access path as connectivity evolves. Evaluate whether existing sessions recover and whether delayed messages still satisfy their application deadlines.',measure:'Interruption duration · Session recovery · Deadline compliance',path:'M85 95 L285 160 L495 95 L625 180'},outage:{label:'LINK OUTAGE',status:'Degraded condition',title:'Observe recovery when a link is lost.',copy:'Remove the link from Relay A to the gateway. An alternate path illustrates possible rerouting; actual recovery depends on the configured routing protocol and link availability.',measure:'Recovery time · Delivery ratio · Application backlog',path:'M85 95 L285 160 L495 95 L625 180'},congestion:{label:'BACKGROUND TRAFFIC',status:'Competing demand',title:'Evaluate timeliness under load.',copy:'Add bulk transfers and routine telemetry to the reference scenario. Observe queuing, TCP behavior, and whether priority messages still reach the receiver on time.',measure:'Tail latency · Deadline misses · Throughput by traffic class',path:'M85 95 L285 55 L495 95 L625 180'}};
document.querySelectorAll('[data-scenario]').forEach(button=>button.addEventListener('click',()=>{const key=button.dataset.scenario,s=scenarios[key];document.querySelectorAll('[data-scenario]').forEach(b=>{b.classList.toggle('active',b===button);b.setAttribute('aria-pressed',String(b===button))});document.getElementById('scenario-title').textContent=s.label;document.getElementById('scenario-status').textContent=s.status;document.getElementById('effect-title').textContent=s.title;document.getElementById('effect-copy').textContent=s.copy;document.getElementById('effect-measure').textContent='OBSERVE / '+s.measure;document.getElementById('route').setAttribute('d',s.path);document.getElementById('failed').style.display=key==='outage'?'block':'none';document.getElementById('route').style.animationDuration=key==='congestion'?'6s':'2s'}));
const canvas=document.getElementById('orbit'),ctx=canvas.getContext('2d'),reduce=matchMedia('(prefers-reduced-motion: reduce)');let paused=reduce.matches,frame=0,w=0,h=0,t=0;const motion=document.getElementById('motion');function syncButton(){motion.textContent=paused?'Resume motion ▷':'Pause motion Ⅱ';motion.setAttribute('aria-pressed',String(paused))}syncButton();function resize(){const dpr=Math.min(devicePixelRatio||1,2);w=canvas.clientWidth;h=canvas.clientHeight;canvas.width=w*dpr;canvas.height=h*dpr;ctx.setTransform(dpr,0,0,dpr,0,0);draw()}new ResizeObserver(resize).observe(canvas);function project(x,y,z){const a=.32;return [w*.51+(x*Math.cos(a)+z*Math.sin(a)),h*.5+y*.94-(z*Math.cos(a)-x*Math.sin(a))*.16,z]}
function draw(){ctx.clearRect(0,0,w,h);const r=Math.min(w*.34,h*.35);ctx.lineWidth=.7;for(let latitude=-75;latitude<=75;latitude+=15){const lat=latitude*Math.PI/180;ctx.beginPath();for(let j=0;j<=120;j++){const a=j/120*Math.PI*2,p=project(r*Math.cos(lat)*Math.cos(a),r*Math.sin(lat),r*Math.cos(lat)*Math.sin(a));j?ctx.lineTo(p[0],p[1]):ctx.moveTo(p[0],p[1])}ctx.strokeStyle='#24445880';ctx.stroke()}for(let meridian=0;meridian<180;meridian+=15){ctx.beginPath();for(let j=0;j<=120;j++){const a=j/120*Math.PI*2,b=meridian*Math.PI/180,p=project(r*Math.cos(a)*Math.cos(b),r*Math.sin(a),r*Math.cos(a)*Math.sin(b));j?ctx.lineTo(p[0],p[1]):ctx.moveTo(p[0],p[1])}ctx.strokeStyle='#24445870';ctx.stroke()}const nodes=[];for(let plane=0;plane<4;plane++){const inclination=(plane*39+20)*Math.PI/180,rad=r*(1.21+plane*.018);function point(a){return project(rad*Math.cos(a)*Math.cos(inclination)-rad*.38*Math.sin(a)*Math.sin(inclination),rad*Math.cos(a)*Math.sin(inclination)+rad*.38*Math.sin(a)*Math.cos(inclination),rad*.7*Math.sin(a))}ctx.beginPath();for(let j=0;j<=150;j++){const p=point(j/150*Math.PI*2);j?ctx.lineTo(p[0],p[1]):ctx.moveTo(p[0],p[1])}ctx.strokeStyle=plane===1?'#b692505c':'#668b9b45';ctx.stroke();for(let n=0;n<7;n++){const p=point(n/7*Math.PI*2+t+plane*.3);nodes.push(p);ctx.fillStyle=plane===1?'#f0bc70':'#8cbbca';ctx.shadowColor=ctx.fillStyle;ctx.shadowBlur=9;ctx.beginPath();ctx.arc(p[0],p[1],2.5,0,Math.PI*2);ctx.fill();ctx.shadowBlur=0}}for(let i=0;i<nodes.length;i++){for(let j=i+1;j<nodes.length;j++){const a=nodes[i],b=nodes[j],d=Math.hypot(a[0]-b[0],a[1]-b[1]);if(d<r*.57&&d>r*.2&&a[2]>0&&b[2]>0){ctx.beginPath();ctx.moveTo(a[0],a[1]);ctx.lineTo(b[0],b[1]);ctx.strokeStyle='#70b7c625';ctx.stroke()}}}ctx.fillStyle='#7394a7';ctx.font='10px monospace';ctx.fillText('CONCEPTUAL ORBITAL GEOMETRY',w*.2,h*.9)}function animate(){if(!paused){t+=.0015;draw()}frame=requestAnimationFrame(animate)}motion.addEventListener('click',()=>{paused=!paused;syncButton()});reduce.addEventListener('change',e=>{paused=e.matches;syncButton()});resize();animate();
